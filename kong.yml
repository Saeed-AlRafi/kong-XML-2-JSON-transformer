_format_version: "3.0"
_transform: true
services:
  - host: www.example_site,com
    name: example_service
    port: 80
    protocol: http
    routes:
    - name: example_route
      paths:
      - /mock
      strip_path: true
plugins:
- name: xml2json
  config:
    enabled: true #is true by default.
